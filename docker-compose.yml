
services:
  fetcher:
    build:
      context: .
    environment:
    # gdzie mają trafić logi -dla pliku: 'file', dla obu 'both' 
      - LOG_MODE=console
    #  - PYTHONUNBUFFERED=1
    image: f1_data_fetcher
    volumes:
      - ./input:/app/input
    env_file:
      - .env
    command: --input /app/input/params.json
